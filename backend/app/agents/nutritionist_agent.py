"""
PLACEHOLDER. Kaveesha replaces this file with the real Nutritionist Agent
(see the instructions in the documentation kaveesha aiyee).

The rest of the backend only depends on the function name `generate_meal_plan`
and the keys in its returned dict - keep both exactly as they are below when
building the real version, so nothing else needs to change.
"""


def generate_meal_plan(mother_id: str, reports: list[dict], monitoring_history: list[dict]) -> dict:
    return {
        "trimester": None,
        "plan_sinhala_msg": "ආහාර සැලැස්ම තවම සකස් කර නොමැත.",
        "warning_sinhala_msg": None,
    }