"""
PLACEHOLDER. Kaveesha replaces this file with the real Report Analysis Agent
(read the documentation file kaveesha aiyeeee :-)).

The rest of the backend only depends on the function name `analyze_report`
and the keys in its returned dict - keep both exactly as they are below when
building the real version, so nothing else needs to change.
"""


def analyze_report(file_path: str, report_type: str) -> dict:
    return {
        "hb_level": None,
        "blood_sugar": None,
        "urine_protein": None,
        "risk_level": None,
        "explanation_sinhala": "මෙම වාර්තාව තවම විශ්ලේෂණය කර නොමැත. කරුණාකර පසුව නැවත පරීක්ෂා කරන්න.",
    }